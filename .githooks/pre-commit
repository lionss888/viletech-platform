#!/usr/bin/env bash
# Repo pre-commit: same surface as `make -C vdp precommit-gate`
# (env-parity → docs-format + unit + management Telegram on fail).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec make -C "$ROOT/vdp" precommit-gate
