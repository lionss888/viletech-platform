export enum TreasurerTaskType {
  REFUND_PAYMENT = 'refund_payment',
}

export enum TreasurerTaskStatus {
  CREATED = 'created',
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}
