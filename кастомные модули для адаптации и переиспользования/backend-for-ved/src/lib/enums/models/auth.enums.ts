export enum AuthPattern {
  VERIFY_ACCOUNT = 'auth.verify.account',
  VERIFY_ACCOUNT_BY_TOKEN = 'auth.verify.account.by.token',
  VERIFY_CRYPTO_ACCOUNT_BY_TOKEN = 'auth.verify.crypto.account.by.token',
}
