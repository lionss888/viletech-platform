Конструктор сценариев — проверка среды
Дата: 2026-09-17
Контур: продукт кабинетов (vdp), localhost
Вид работы: запуск / проверка среды
Итог: частично прошло (среда и рычаги ролей — да; живые CTA на заявках — нет данных)

────────────────────────────────────────
Сводка

Статус: частично прошло
Стек кабинетов поднят. Пилотные роли ICO/ECO выключены. Менеджер не может менять process-roles (403). Живых заявок в API нет — ветки corrections / refund / shipment и денежные маршруты на CTA в браузере в этом прогоне не проверены (опора: доменный документ + код лестниц).

────────────────────────────────────────
Чеклист

1. Среда поднята
Результат: прошло
Смотреть: docker — vdp-fe, vdp-core, hub, postgres и смежные сервисы healthy; FE http://localhost:5173 → 200; core /api/v1/health → 200; страница входа /login → 200; /process-roles → 200 (HTML).

2. Пилот: ICO и ECO выключены
Результат: прошло
Смотреть: GET /api/v1/process-roles под root — internal_compliance_officer enabled=false; compliance_officer enabled=false; version=28. Обязательные actor-роли user/manager/provider на месте.

3. process-roles: только root меняет слоты
Результат: прошло
Смотреть: PUT /api/v1/admin/process-roles/internal_compliance_officer под manager@vdp.local → 403 Forbidden. Менеджер сделки слоты не переключает.

4. Денежные маршруты в рамках готовых лестниц
Результат: не проверено на живой заявке (нет forms в API: user и manager — пустые списки)
Смотреть при появлении данных: создание/выбор advance, post_payment / POSTPAY_RATE_ON_PP, export PAY_FROM_EXPORT.
Канон в коде/доках: vdp/docs/domain/form-lifecycle.md (лестницы описаны и покрыты тестами).

5. Ветки corrections / refund / shipment — CTA и допустимый переход
Результат: не проверено на живой заявке (нет forms)
Смотреть при появлении данных: reject → corrections; refund CTA при FundsHeld; shipment опционально.
Канон: тот же form-lifecycle.md + ActionPanel / pilot-matrix specs.

6. Mid-flight: смена process-roles не мигрирует статусы идущих заявок
Результат: не проверено на живой заявке (нет forms); инвариант version stamp — по исследованию и домену
Смотреть: после смены конфига root’ом статус существующей заявки не прыгает «сам».

────────────────────────────────────────
Что смотреть дальше

- Посеять или создать хотя бы одну заявку на localhost и повторить пункты 4–6 руками.
- Не трактовать пустую очередь как «рычагов нет» — рычаги ролей и AuthZ уже подтверждены.
- Браузерный логин root в этом прогоне не использовался (достаточно API); UI process-roles открывается по URL.

────────────────────────────────────────
Вывод для программы

Среда пригодна для этапа 1 по ролям процесса и границе прав менеджера.
Полный прогон операционных веток и лестниц на CTA — отложен до появления заявок в среде.
