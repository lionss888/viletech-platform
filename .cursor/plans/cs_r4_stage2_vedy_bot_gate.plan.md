---
name: CS-R4 stage2 vedy gate
overview: Текстовый чеклист перехода к этапу 2 (vedy_bot). Без кода статусной машины VDP и без runtime-подмены ботом.
todos:
  - id: r4-checklist-file
    content: Создать заметки/конструктор-сценариев/чеклист-этап-2.txt
    status: completed
  - id: r4-link-master
    content: Ссылка на vedy_bot_master + оглавление папки конструктора
    status: completed
  - id: r4-human-accept
    content: Приёмка человеком текста чеклиста (ок / правки)
    status: completed
isProject: false
---

# CS-R4 — чеклист входа в этап 2 (vedy_bot)

Родитель: [конструктор_реализация_889c3d6f.plan.md](конструктор_реализация_889c3d6f.plan.md).  
Бот: [vedy_bot_master_knowledge_agent.plan.md](vedy_bot_master_knowledge_agent.plan.md).  
Опора: [решение-согласование.txt](заметки/конструктор-сценариев/решение-согласование.txt), [схема-устройства.txt](заметки/конструктор-сценариев/схема-устройства.txt).

## Цель

Явный чеклист: когда запрос менеджера **не** закрыт рычагами этапа 1 → запрос через vedy_bot / intake → план кастома → релиз VDP. Бот не меняет статусы кабинетов «на лету».

## Слои

| Слой | IN / OUT |
|---|---|
| Docs / тексты (`заметки/`) | IN |
| UI / FE / домен / API / unit / E2E | OUT |

## Содержание чеклиста (файл)

Путь: `заметки/конструктор-сценариев/чеклист-этап-2.txt`

Прогнать запрос по порядку; в этап 2 только если **нет**:

1. process-roles (через root) закрывает участие ролей;
2. существующий денежный маршрут (advance / postpay / export);
3. существующие ветки (corrections / refund / shipment / provider return);
4. continuity менеджера на тех же статусах.

Тогда: канал vedy_bot → разбор → отдельный plan разработки → релиз. Ссылка на мастер бота. Явно: не подмена form-payment ботом.

Обновить [оглавление.txt](заметки/конструктор-сценариев/оглавление.txt).

## DoD

- Файл чеклиста написан простым языком.
- Человек: ок / правки.
- Нет diff в `vdp/**` статусной машины.

## Вне scope

Реализация кастома в core; BPM; передача process-roles менеджеру; исполнение P0–P5 бота в этой волне (только указатель).

## Сверка rules

`поддержка-и-обратная-связь`, `интеграция-и-события`, `честность-готовности`, `use-cases`.
